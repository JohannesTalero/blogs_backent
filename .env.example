# Supabase — Settings > API en el Dashboard
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_KEY=<service_role_key>   # usar service_role, NO anon

# JWT — generar con: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=<secret_aleatorio_minimo_32_chars>
JWT_EXPIRE_HOURS=1
